# 开发任务 02：删减审计与本轮改动

日期：2026-09-05。对照原任务 `/home/herman/Documents/Repo/Projects/DSHPlugins/个人Feed/开发任务02.md`。起点为干净的 `c3eab74`。删减实施阶段只改本仓源码、测试和文档；该阶段没有访问或改动真实浏览器、模型、用户服务、宿主配置、历史数据，也没有提交、推送或部署。用户随后要求实际验证，结果另见 [真实运行验证](task02-live-verification-2026-09-05.md)。

**第一批确定删减已实施并通过同一审计者收口：B1–B5 全部关闭，局部 PASS；整个功能是否已经足够简单仍为 REVISE。** 本文不把单次 MCP 返回或自动测试当作开发任务 02 的真实用户验收。

## 1. 用户结果与硬约束

| 用户结果 | 硬约束 | 当前直接事实 | 降为候选的机制 |
|---|---|---|---|
| R1：普通入口请求，系统自行看完整有界 X 原文，给 0–1 条 | R4：个人价值、关注匹配和信息增量同时成立；不足/失败不能是业务空 | E2：任务02；E9–E10：Python/CDP观察链；E16：不足正文曾被跳过 | 固定三面、上海日切、跨进程身份回传、永久processed |
| R2：两类个人语境持续更新，跨会话与重启可用；曝光不等于认识 | R5：独立、通道无关，不依赖宿主存储 | E6/E17：原话hash会跳过关注回归；E8：反馈日志未用于选择 | hash历史、乐观版本、反馈历史、状态结构 |
| R3：本轮删除冗余，不为未来需求扩建 | R6：五MCP工具及封闭类别稳定 | E5：request是候选记录的唯一串行写路径；E11：DSH安装责任进入包 | 嵌套候选队列、版本检查、宿主安装生命周期 |
| R9：真实入口、个人价值和两类长期变化仍需真实证据 | R7：单用户loopback服务及隐私边界；R8：隔离测试、先失败再修改，不部署 | E13：历史一次MCP返回不等于完整验收；E18–E19：未使用接口和常量 | 常驻验收流程、完整性审计系统、测试专用生产接口 |

编号沿用本轮冻结证据包。原任务中的用户结果保持有效；旧设计、验收定标和历史 PASS 不自动成为机制必须保留的理由。

## 2. 被降为候选的设计与流程

已删除：候选嵌套队列及记录条数版本协议；最近 10000 条原话 hash 历史；DSH 专用安装、配置归属、Skill 目录替换和回滚代码；`networkProbe` 占位接口、`track` 的未用参数、Python 中没有运行时读取者的 `TARGETS`。

仍在实现中、尚未证明必需：永久 `candidates.jsonl` 及已判定对象永不再判；跨进程浏览器锁；固定三面与上海日切协议；服务安装备份和归属生命周期；反馈历史与收藏事件重放。它们进入后续删减候选，不因现存就成为免费基线。

旧材料中的全量终端总体、七日 capture 审计、UNKNOWN 批次和多阶段盲审，降为历史候选流程。本轮没有重建它们，也没有删除历史记录或改写过去的用户授权。若再次需要某机制，须指出当前结果与现实失败，不凭旧文档续建。

## 3. 全局最小方案与更小反事实

当前候选最小路径为：通用 Agent/MCP 接入一个本机服务；用获准普通事实更新长期关注和已有认识；完成一次有界 X 原文观察；按个人价值、关注和增量保守选择；返回一条、业务空或未完成。反馈续接和收藏只按现有五工具合同保留，不扩成学习流水线。

什么都不新增可以支持本轮删减。让用户每次提供候选或画像会删掉 R1/R2，不能算满足同一结果的更小方案。一次性通用 Client 配置足以替代 Feed 自己管理 DSH；一个请求队列足以替代对同一唯一写者的两层保护。正文不足用现有 `incomplete/source_window` 就能如实表示，无需补采、重试或新状态。

## 4. 审计对象、预算基线、概念与覆盖

对象保持 `whole_feature`，预算基线为 `[]`，没有把已提交代码洗成基线。目标候选按低预算反证后的较细口径列 13 项；这不是当前所有代码已最小化的声明：

| 概念 | 对应结果/约束 |
|---|---|
| C1 独立本机 MCP 服务 | R1、R5、R7 |
| C2 五工具固定合同 | R6 |
| C3 鉴权和秘密边界 | R6、R7 |
| C4 完整且正文足够的有界 X 观察 | R1、R4 |
| C5 持续更新的长期关注 | R2 |
| C6 独立于曝光的已有认识 | R2 |
| C7 个人长期价值条件 | R1、R4 |
| C8 变化后的长期关注匹配条件 | R1、R2 |
| C9 相对已有认识的信息增量条件 | R4 |
| C10 一条、业务空和未完成的区别 | R4、R6 |
| C11 明确语义反馈的真实处理记录 | R6 |
| C12 反馈追问的显式 token 续接 | R6 |
| C13 收藏当前状态与查询 | R6 |

R3、R8、R9 是本轮删除、执行与结论约束，不为它们新造产品机制。当前仍有未举证的旧机制留存，所以不给整个功能最小性的 PASS。

## 5. 增加阶梯、逐个删除与机械校验

临时预算为 `/tmp/personal-feed-simplification-audit/executor-budget.json`，保存 S0→S13，每步只增一个 C；逐项删除复现相同 R/E 对应失败，并写明人工恢复或一次性替代为何不足。

阶梯依次补足：独立入口→稳定工具合同→鉴权→自行完整观察→长期关注→已有认识→个人价值→关注匹配→信息增量→诚实结果→真实反馈→追问续接→收藏。删除相应项会分别失去该项结果。这个必要性比较不证明世界上不存在其他更小设计。

从 Skill 根目录运行 `python3 scripts/validate_complexity_budget.py /tmp/personal-feed-simplification-audit/executor-budget.json`，结果为 `PASS`、`concept_count: 13`、`errors: []`。机械校验只证明格式、引用、覆盖和数量守恒，不证明原子性、最小性或用户验收。

## 6. 独立低预算挑战与复杂度差额

一名独立审计者与一名 Luna 挑战者先从同一证据集形成方案，再对照执行者候选。挑战者初稿为 10 项，机械校验通过，但混合了服务/鉴权、反馈记录/token续接，且未单列关注匹配。经具体 R6/E3/E14 和 R1/E2 反例对照，挑战者接受拆分并撤回“10 是下限”；修正为 13 项目标口径。

双方对删减内容一致。本轮新增长期机制为 0：删去一个候选队列、一组版本协议、一份 hash 集合及容量规则、宿主安装/归属/回滚责任和无行为接口；没有增加模块、状态字段、后台任务、迁移器或补偿流程。反馈和收藏的存在依据是 R6 合同约束，不能假称它们已证明长期学习效果。

## 7. 冻结关闭条件与实施

| 原阻塞 | 最小改动与通过条件 | 实施结果 |
|---|---|---|
| B1 同一候选写路径维护两层并发协议 | 删 candidateQueue/expectedGeneration，保留 requestQueue；并发请求不重复选同一账本对象 | 已删除；共享候选并发测试保护结果 |
| B2 原话缓存阻止可靠更新 | 删 observed/hash/10000规则；A→非A→A可恢复关注，继续读取原facts | 已先复现失败，再删除缓存；五工具类别不变 |
| B3 包内维护宿主安装 | 删除DSH安装/回滚；保留通用endpoint/Bearer/serverName/现有Skill及独立服务安装 | 已删除专用安装器、辅助函数及专用测试；两份README改为通用接入 |
| B4 无行为的生产测试接口 | 删除占位参数和无运行时读取者常量，保持实际行为测试 | 已删除；一个Python fixture改用字面导航URL，不再借用生产死常量 |
| B5 不足正文被误报完成 | complete窗口含insufficient时返回现有source_window/incomplete | 已先复现complete+空数组，再修正分支 |

D5 永久 processed 和 D6 历史流程本轮仅降为候选；现有账本与跨次跳过语义保留。原话 hash 移除后，每次语境调用都会重新经过模型观察，这是换掉错误历史缓存的直接成本；不添加替代缓存。旧个人语境文件的 facts/generation 仍可读取，多余 observed 字段被忽略。

验证记录：先有 29 项相关测试通过；新增反例实际出现 3 项失败（关注回归、正文不足、包边界），修改后 33 项定向测试通过。最终 `nix develop --command pnpm check` 全部通过：类型检查、41 项 TypeScript 测试、28 项 Python 测试和构建。构建仍有历史存在的 `define` 选项警告。`git diff --check` 通过。

## 8. REVISE

整个功能仍为 REVISE：永久 processed、其他历史生命周期及完整长期效果仍未充分举证。这一结论要求继续删除、降级或延后，不要求新增机制；本轮 B1–B5 的局部工作按冻结条件收口。没有宣称整个仓库达到全局最优，没有以测试替代真实入口、用户价值确认或两类长期变化验收。

同一独立审计者最终核对实际代码、测试和接入文档，B1–B5 全部 CLOSED，未发现本轮修改引入的新阻塞。临时收口记录：`/tmp/personal-feed-simplification-audit/reviewer-close.md`。生产代码新增 35 行、删除 387 行，净删 352 行；该数字不含测试与文档。
