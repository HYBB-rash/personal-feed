# D02 实施与验证记录

日期：2026-09-06。分支：`codex/d02-context-updates`。代码候选：`b053e5e2979f9f77c5e65b8aa625650cab51d3e3`；集成基线：`f0b972de8ad7136494b10a5689874e7ea0552776`（已包含 D01）。本记录随后以单独的本地文档提交交付。

## 1. 用户结果与硬约束

R1：准确新增、纠正、撤回，保留无关信息并准确计数。R2：保存存疑认识，但不把它当成已知信息参与筛选。R3：当次请求立即使用更新，跨进程保留。R4：维持五工具、闭合结果类别及独立服务边界。R5：独立工作树、先失败测试、提交后 rebase、验证最终代码。

交付限于 D02 分支的本地提交；没有合入 main、推送或部署。原主工作树的未跟踪 `docs/d02-gap-analysis.md` 保留。

## 2. 被降为候选的设计与流程

未引入持久事实 ID、新数据库、迁移、操作账本、澄清生命周期或恢复服务。D02 不增加问题、token 或自动续做行为；集成时保留 D01 已加入的接入字段及其校验。

## 3. 全局最小方案与更小反事实

内部模型输出 `changes: { additions, replacements }`。替换携带完整旧事实作为目标，空列表表示撤回，多条输出支持确定与存疑部分的拆分。应用整批校验后使用既有队列、generation 检查及原子写入提交 v1 快照；无效或冲突的批次不部分写入。

请求准备时过滤存疑认识，再执行现有必要信息检查及候选判断；维护仍读取完整事实。计数基于批次的最终事实，重复及抵消操作不虚增数量，一个有效替换目标即使拆分仍计一项。继续使用旧合并规则会残留被纠正的认识；人工修改快照无法满足日常表达更新及当次生效。

## 4. 审计对象、预算基线、概念清单与需求覆盖

冻结审计对象仍是从 `51614142c0737c1c399d85a50ebf8a7ac6313422` 开始的 D02 增量。D01 进入主线只改变集成基线，不把已经实现的 D02 能力算成既有能力。

| 概念 | 责任 | 需求 |
|---|---|---|
| C1 定点修改 | 明确目标、整批校验与替换/撤回 | R1 |
| C2 存储事实与筛选可用事实的区别 | 保留疑问并从筛选输入排除 | R2 |
| C3 实际变更数量 | 新增、替换、撤回及无变化的准确计数 | R1 的结果准确性 |

既有事实类型、v1 存储、原子写入、队列及冲突检查支撑 R3；既有五工具、隔离测试和本次工作树流程支撑 R4/R5。没有新增服务或长期协调机制。

## 5. 增加阶梯、逐个删除与机械校验

S0 依次加入 C1、C2、C3：分别关闭旧认识残留（E1）、存疑认识进入筛选（E2）、同长度修改报告零（E3）。删除任意一项都会重现对应反例。执行者、独立审计者及挑战者的三份预算在计划阶段均通过机械校验，最终预算为三个概念；计数修复只是 C3 内部计算。

## 6. 低预算挑战与反例

Luna 起初将计数并入定点修改，提出两个概念。核对“状态正确修改，但仍报告零”的独立失败后，接受 C3 单独计数；最终复杂度差额为零。实现沿用同一审计者有限复核，没有重开盲审或新增评审流程。

## 7. 阻塞项、受影响范围与关闭证据

| 冻结条件 | 证据 |
|---|---|
| E1：正确撤改，保留无关事实 | 改写纠正、撤回、关注变化及恢复、部分拆分；整批无效目标/重复目标/输出冲突不提交；并发与写入失败保护 |
| E2：疑问保留但隔离 | 存疑事实仍在快照及维护输入；候选判断不接收它；只有存疑认识时不进入来源观察；明确新手陈述可使用 |
| E3：准确报告变化 | 新增、重复、同长度替换、撤回、拆分及等值替换；审查新增三个抵消/重复批次反例，先失败后通过 |
| 必要回归 | 更新用于同一次请求；新 Node 进程重读；普通更新不启动 Feed；推荐、收藏及未明确缘由的不喜欢不自动增加认识；D01 接入回归通过 |

测试运行真实应用提交、读取、过滤和持久化路径，使用临时目录及受控模型/观察器。模型解码测试直接验证受控 HTTP 响应。未接触真实浏览器账户、模型端点或运行服务。

TDD 记录：模型协议新增测试先失败，随后 10/10 通过；上下文行为测试先复现旧实现缺口；最终计数三个反例实测分别错报 1、2、2（预期 0、0、1），修复后 3/3 通过，完整上下文测试 23/23 通过。

在 rebase 后代码候选上运行 `nix develop --command pnpm check`，退出码为 0：

- TypeScript 类型检查通过；12 个测试文件共 124 项通过，其中包括 D01 接入 39 项、应用合同 17 项、D02 上下文 23 项、模型 10 项。
- Python 28 项通过，构建通过。
- 构建仍提示 `define` 选项告警；在隔离的原始 `5161414` 构建中已复现相同告警且退出码为 0，未改动构建工具。

收尾时 SSH agent 无法签名，随后对同一 GitHub 仓库通过 HTTPS fetch 成功刷新远端引用，确认 `origin/main=5161414`；本地 `main=f0b972d` 是其后代。D02 rebase 到本地 main 无冲突，并完成上述集成验证。

## 8. 最终结论

**PASS。** 同一独立审计者已对代码候选 `b053e5e` 完成有限复核，确认 E1/E2/E3 均关闭、预算仍为三个概念，没有未关闭阻塞。此结论只放行 D02 本地分支交付，不增加合并、推送或部署授权。

验证范围是受控模型下的应用行为与接入兼容。真实对话提取质量、真实 X 来源体验仍需用户验收；具体领域的信息充分性、澄清生命周期和自动续做属于后续任务。
